Sources for the plugin `plugin.advertisingId`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.advertisingId" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
